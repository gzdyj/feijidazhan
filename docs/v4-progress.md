# 第四版开发进度记录

> 每完成一步更新此文档，记录完成内容、当前状态、下一步计划。

---

## 当前进度

**当前步骤**：全部完成 ✅
**总体进度**：5/5
**最后更新**：2026-08-13

---

## 步骤详情

### ✅ 步骤1：根因 Review
- 通读 index.html（运行时版本），定位"越到后面越糊"四大根因（DPR/阴影/实体无上限/每帧开销）
- 确认 v2/v3 功能均已实现，触屏模式移动逻辑缺失为 v2 遗留问题

### ✅ 步骤2：DPR 高清渲染
- resize() 改为 DPR 感知 + setTransform，逻辑坐标保持 CSS 像素
- 背景渐变缓存（buildBg，resize 时重建一次）

### ✅ 步骤3：画质自适应系统
- 4 档 QUALITY_LEVELS（阴影/DPR/粒子/星数）
- 60 帧窗口实测帧率，<40fps 降档、>55fps 升档（滞回防抖动）
- 全部 shadowBlur 收敛到 SB() 开关

### ✅ 步骤4：实体上限 + 移动端体验
- 玩家子弹/敌机/道具/飘字/敌弹上限
- 触屏跟随修复、touch-action:none、切后台自动暂停、iOS 音频解锁、射击音效节流、触屏默认操作

### ✅ 步骤5：测试验证
- node --check 语法校验通过
- DOM/Canvas 桩冒烟测试：DPR 生效(780x1688)、500帧触屏对战、score 增长、自动暂停、必杀技、退出菜单 全部通过
- 自适应升降级全周期测试：780→585→390→585→780 通过

---

## 变更记录

- 2026-08-13：完成 v4 全部步骤，index.html 通过全部测试
- 2026-08-13：v4.2 手感与功耗优化
  - 道具时长加长：双倍火力 5s→10s、穿透弹 6s→10s、双倍分数 8s→10s、护盾 5s→7s、急速 3s→4s（激光/三连发等武器爽更久）
  - 修复：满级武器捡"双倍火力"过期后会掉级 —— 改为记录拾取前等级、过期精确回退
  - 功耗优化（手机发热）：
    - 背景预渲染：渐变+静态星野渲染到离屏 canvas，每帧仅一次 blit（替代全屏渐变填充+120颗星逐颗绘制）
    - 粒子上限下调（300→240/260→200/200→160/120→100）
    - 音效节流：爆炸 40ms、命中 20ms（减少噪声缓冲区与音频节点分配）
    - 暂停时冻结画面不再持续重绘
    - 自适应降级阈值 <40fps→<45fps（更早介入）
  - 新增设置项"画质"：自动/高/中/低，手动档位直接生效且不被自适应突破
- 2026-08-13：v4.1 平衡与手感调整
  - BOSS 弹伤害体感修复：护盾改为"挡一次即破碎"、敌弹碰撞半径 +16→+22、无敌帧期间子弹穿过不吞弹
  - 节奏加快：出兵间隔 `max(12,50-wave*2)` → `max(10,44-wave*4)`；每波敌机 `5+wave*2` → `5+wave*3`
  - 上强度：高威胁敌机提前解锁（自爆 w3/坦克 w5/狙击 w6）且概率上调；敌机速度随波次 +0.05/波（上限 +1.5）
  - BOSS 强化：血量 `30+wave*6`、散射 12→16 弹、十字 8→10 向、攻击间隔 50→40 帧
